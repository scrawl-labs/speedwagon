{
  "name": "@scrawl-labs/speedwagon-grafana",
  "version": "0.2.0",
  "description": "Grafana MCP server — dashboard search, panel details, and metric queries via Grafana HTTP API.",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "speedwagon-grafana": "./dist/index.js"
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/index.js",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "@scrawl-labs/speedwagon": "workspace:*",
    "zod": "^3.25.23"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  },
  "keywords": ["mcp", "grafana", "metrics", "dashboards", "prometheus", "speedwagon"],
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  }
}
