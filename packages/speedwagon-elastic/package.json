{
  "name": "@scrawl-labs/speedwagon-elastic",
  "version": "0.2.0",
  "description": "Elasticsearch MCP server — log search, error aggregation, and index discovery via Elasticsearch REST API. Read-only.",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "speedwagon-elastic": "./dist/index.js"
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/index.js",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "@scrawl-labs/speedwagon": "workspace:*",
    "zod": "^3.25.23"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  },
  "keywords": ["mcp", "elastic", "elasticsearch", "logs", "errors", "speedwagon"],
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  }
}
