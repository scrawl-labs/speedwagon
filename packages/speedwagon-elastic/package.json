{
  "name": "@scrawl-labs/speedwagon-elastic",
  "version": "0.1.0",
  "description": "Elastic (APM + logs) MCP server in the Speedwagon family. Read-only gateway over the Kibana Agent Builder MCP.",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "speedwagon-elastic": "./dist/index.js"
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/index.js",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@scrawl-labs/speedwagon": "0.1.0"
  },
  "keywords": ["mcp", "elastic", "elasticsearch", "apm", "logs", "speedwagon"],
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  }
}
