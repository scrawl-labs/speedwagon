{
  "name": "@scrawl-labs/speedwagon-mongodb",
  "version": "0.1.0",
  "description": "MongoDB query analyzer MCP server (audit). Read-only by construction — production-safe.",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "speedwagon-mongodb": "./dist/index.js"
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/index.js",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "@scrawl-labs/speedwagon": "0.1.0",
    "mongodb": "^6.16.0",
    "zod": "^3.25.23"
  },
  "keywords": ["mcp", "mongodb", "explain", "query", "analyzer", "read-only", "speedwagon"],
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  }
}
